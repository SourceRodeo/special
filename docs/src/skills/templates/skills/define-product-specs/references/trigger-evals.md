@filedocuments spec SPECIAL.SPEC_COMMAND
@implements SPECIAL.DOCUMENTATION.SKILLS.FALLBACK.SPECS
@applies DOCS.SKILL_TRIGGER_EVAL_REFERENCE

# Trigger Evals

## Should Trigger

- Turn this roadmap item into concrete product claims before we start coding.
- Write the expected behavior for this new feature before we implement it.
- Rewrite this claim so it states the contract instead of the implementation.
- Split this vague requirement into smaller claims.

## Should Not Trigger

- Investigate a production outage in the API service.
- Convert this markdown file to HTML.
- Install the published binary with Homebrew.
- Review whether a verify body is self-contained enough.
